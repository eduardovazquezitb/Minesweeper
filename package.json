{
  "scripts": {
    "test:e2e": "cucumber-js --require features/cucumber.conf.js --require features/*.steps.js --format @cucumber/pretty-formatter --tags \"not @manual\" --parallel 4"
  },
  "dependencies": {
    "@cucumber/cucumber": "^8.5.3",
    "@cucumber/pretty-formatter": "^1.0.0",
    "@playwright/test": "^1.25.2",
    "playwright": "^1.25.2"
  },
  "devDependencies": {
    "@stdlib/stats-anova1": "^0.0.7"
  }
}
